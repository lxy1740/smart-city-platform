import { Route } from '@angular/router';
import { IssuedataComponent } from './issuedata.component';

export const IssuedataRoutes: Route[] = [

    {
        path: 'issuedata',
        component: IssuedataComponent

    }
];
