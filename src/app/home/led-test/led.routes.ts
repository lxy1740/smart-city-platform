import { Route } from '@angular/router';
import { LedTestComponent } from './led-test.component';

export const LedRoutes: Route[] = [

    {
        path: 'led',
        component: LedTestComponent,

    }
];
